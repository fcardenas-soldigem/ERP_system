Lee ~/.claude/skills/gstack/investigate/SKILL.md y ~/.claude/skills/gstack/careful/SKILL.md y aplica ambos skills.

BUG CRÍTICO: Las compras se importan pero con montos incorrectos. Ejemplo: una compra de ALLTEK que debería ser S/1,400+ aparece como $1.40, una de INTCOMEX que debería ser S/120+ aparece como $0.12. Todos los montos están divididos entre 1000 aproximadamente, o el mapeo de columnas está leyendo la columna incorrecta.

PASO 1 — INVESTIGAR:

1. Lee backend/apps/importador/services.py — busca el método que importa compras
2. Identifica EXACTAMENTE qué columna del Excel se está usando para:
   - precio_unitario del CompraDetalle
   - subtotal de la Compra
   - igv de la Compra  
   - total de la Compra
3. Lee el Excel estructurado de referencia. Las columnas de compra en el Excel son:
   - PRECIO_UNIT_SIN_IGV → precio unitario sin IGV (ej: 60.00)
   - IGV_UNIT → IGV por unidad (ej: 10.80)
   - PRECIO_UNIT_CON_IGV → precio unitario con IGV (ej: 70.80)
   - SUBTOTAL_SIN_IGV → total sin IGV (cantidad × precio_unit) (ej: 1200.00)
   - IGV_TOTAL → IGV total (ej: 216.00)
   - TOTAL_CON_IGV → total con IGV (ej: 1416.00)
4. Compara con lo que el ImportService realmente lee — ¿está leyendo la columna correcta?

CAUSAS PROBABLES:

A. El mapeo fuzzy de columnas está mapeando "PRECIO_UNIT_SIN_IGV" a un campo incorrecto o no lo encuentra y usa un default de 0.01
B. Los valores se están leyendo como string y no se parsean a Decimal/float correctamente
C. El ImportService crea la compra sin detalles y el total se calcula como 0 o mínimo
D. La moneda está en USD ($) en vez de PEN (S/) — en el screenshot dice "$0.01" no "S/0.01"
E. Los precios del Excel están en una columna que pandas lee con nombre diferente al esperado (espacios, tildes, mayúsculas)

PASO 2 — DEBUG RÁPIDO:

Agrega prints temporales en el ImportService justo antes de crear cada CompraDetalle:
- print(f"Fila: {fila}")
- print(f"Columnas disponibles: {list(fila.keys())}")  
- print(f"Precio leído: {precio_value} tipo: {type(precio_value)}")
- print(f"Cantidad: {cantidad} Subtotal: {subtotal}")

Esto va a revelar exactamente qué está leyendo.

PASO 3 — CORREGIR:

1. Asegurar que el mapeo de columnas conecte correctamente:
   - PRECIO_UNIT_SIN_IGV o PRECIO DE COMPRA → CompraDetalle.precio_unitario
   - CANTIDAD → CompraDetalle.cantidad
   - SUBTOTAL_SIN_IGV o TOTAL SIN IGV → Compra.subtotal
   - IGV_TOTAL → Compra.igv
   - TOTAL_CON_IGV o TOTAL + IGV → Compra.total

2. Parsear valores correctamente:
   def parse_decimal(valor):
       if pd.isna(valor) or valor is None:
           return Decimal('0')
       try:
           return Decimal(str(valor).replace(',', ''))
       except:
           return Decimal('0')

3. Si el modelo Compra auto-calcula total desde los detalles (como actualizar_totales()), entonces:
   - Crear CompraDetalle con precio_unitario y cantidad correctos
   - Llamar compra.actualizar_totales() después de crear todos los detalles
   - NO setear el total manualmente si se auto-calcula

4. Corregir la moneda: debe ser PEN (soles) no USD. Verificar qué valor default pone el ImportService en el campo moneda.

5. Después de corregir, eliminar los prints de debug.

PASO 4 — VERIFICAR:

Muéstrame una compra importada con sus valores reales del Excel para confirmar que los montos son correctos.

Muéstrame la causa raíz exacta y los cambios antes/después.